export default class RawObject {
    width;
    height;
    x;
    y;
    z;
    constructor(x, y, width, height) {
        this.x = x;
        this.y = y;
        this.width = width;
        this.height = height;
    }
}